<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <div class="nav-wrap row">
            <div class="col-4 site-title-wrap ">
                <h1> <a class="site-title" href="/">Tech Blog</a></h1>

            </div>
            <div class="col-8 nav-btn-wrap">
                <nav>
                    <div class="row">
                        <div class="col-2"><a href="/">Home</a></div>
                        <div class="col-6"><a href="/dashboard">Dashboard</a></div>
                        {{#if logged_in}}
                        <div class="col-4"><button class="btn btn-danger" id="logout">Log out</button></div>
                        {{else}}
                        <div class="col-4 "> <a class="btn btn-danger" href="/login">Login</a></div>
                        {{/if}}

                    </div>

                </nav>
            </div>

        </div>

    </header>
    <main>
        {{{ body }}}
    </main>


    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>